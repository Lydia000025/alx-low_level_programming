#include <stdio.h>
#include <stdlib.h>
/**
 * main - function
 * Return:0
 */

int main(void)
{
	int l , u;
	
	for (l = 97; l < 123; l++)
	{
		putchar(l);
	}
	for (u = 65; u < 90; u++)
	{
		putchar(u);
	}
	putchar('\n');
		return (0);
}
