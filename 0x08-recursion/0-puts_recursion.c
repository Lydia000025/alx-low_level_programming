#include "main.h"
#include <stdio.h>
/**
 * _puts_recursion - a function that prints a string
 * @s: input character
 */
void _puts_recursion(char *s)
{
	int letter;

	letter = 'A';
		putchar(letter);
	letter = 'l';
		putchar(letter);
	letter = 'x';
		putchar(letter);
	printf ("\n");
}
